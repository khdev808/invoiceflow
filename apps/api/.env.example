DATABASE_URL="postgresql://invoiceflow:invoiceflow_dev@localhost:5434/invoiceflow?schema=public"
JWT_SECRET="change-me-in-production"
PORT=3001
PORTAL_URL="http://localhost:3000/portal"
ADMIN_APP_URL="http://localhost:3000"

# Payments (required for production online payments & subscriptions)
STRIPE_SECRET_KEY="sk_test_placeholder"
STRIPE_WEBHOOK_SECRET="whsec_placeholder"
PAYPAL_CLIENT_ID="paypal_placeholder"

# Email (required for invoice delivery & password reset in production)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@invoiceflow.app"

# Admin bootstrap (set BOOTSTRAP_ADMIN=true once on Render, then disable)
ADMIN_EMAIL="admin@yourcompany.com"
ADMIN_PASSWORD=""
BOOTSTRAP_ADMIN="false"
SEED_DEMO="false"

# Human verification (Cloudflare Turnstile — required in production)
TURNSTILE_SECRET_KEY=""
